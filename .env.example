# Copy this file to `.env` and fill in your real key(s).
# `.env` is git-ignored — never commit real keys.

# Required:
ANTHROPIC_API_KEY=sk-ant-...

# Optional — for cross-model comparison runs:
# OPENAI_API_KEY=sk-...
# OPENROUTER_API_KEY=sk-or-...
